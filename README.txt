This program takes in a text file of unordered letters, then initially puts them into an uneven binary search tree. Then, my program balances out the search tree.

To run:
C: javac BSTesterL6.java
C: java BSTesterL6 L6input1.txt

You could also use the other two files as the input